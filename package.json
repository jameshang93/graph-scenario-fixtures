{
  "name": "graph-scenario-fixtures",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate.js",
    "test": "npm run validate && node scripts/fixture-fetch.js --selftest && node scripts/play-scenario.js --selftest"
  }
}
